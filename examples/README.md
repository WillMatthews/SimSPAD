# Examples

This directory contains examples on how to use SimSPAD with Python and MATLAB.

Both languages support the use of the SimSPAD server, and reading and writing
the local file format: device parameters as a flat JSON object and waveforms as
1-D little-endian float64 NumPy `.npy` arrays. See the top-level README's "File
Format" section for details.